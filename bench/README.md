# Bench

Shared instrument drivers for bring-up scripts.

## Import path

```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '../../../../bench'))
```

## Structure

- `drivers/` — instrument driver modules
- `utils/` — shared utilities
