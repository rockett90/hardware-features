# hardware-features

Hardware feature designs. Each feature lives in `features/<feature-name>/`.

## Quick start
See [CONTRIBUTING.md](CONTRIBUTING.md) for full workflow.

## Repo structure
- `features/` — one directory per feature
- `library/` — shared KiCad library (git submodule)
- `.github/` — workflows, templates, guidelines, agent instructions
